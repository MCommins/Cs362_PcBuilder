<h1>New List</h1>

<% if @part.errors.any? %>
  <div id="error_explanation">
  <h2><%= "#{pluralize(@part.errors.count, "error")}" %> prohibited this list from being saved:</h2>
  <ul>
    <% @part.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  <% end %>

  <%= form_for(@part) do |t| %>
    <%= t.label(:name) %>
    <%= t.text_field(:name) %>
    <br />
    <%= t.label(:part) %>
    <%= t.text_field(:part) %>
    <br />
    <%= t.label(:compatibility) %>
    <%= t.text_field(:compatibility) %>
    <%= t.submit %>
<% end %>
