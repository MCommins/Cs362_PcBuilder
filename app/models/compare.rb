class Compare

  #compares the compatibility of all parts of a pc (currently a mask until we get a better understanding of how to 
  #program the code)
  def compatible?
    3
  end

  #gets part compatibility values from an Sql table (currently a mask function)
  def get_data
    3
  end
end
